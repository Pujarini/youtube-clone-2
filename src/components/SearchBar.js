const SearchBar = () => {
  return (
    <div>
      <input
        type="text"
        placeholder="search"
        className="w-80 outline-none rounded-full h-[30px] p-5 border-slate-400 border"
      />
    </div>
  );
};

export default SearchBar;
